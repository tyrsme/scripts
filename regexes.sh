#!/bin/bash

grep -r sed cd /home/student/scripts/portfolio
#search for lines containing 'sed'
grep -r "^m"
#search for lines starting with 'm'
grep -r 
#search for lines that cpntain 3-digit numbers
grep -r
#search for lines with echo statements with at least 3 words
grep -r
#search for lines that would make a good password